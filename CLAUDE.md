# CLAUDE.md

Guidance for AI assistants working in this repository.

## What this repository is

This is a **GitHub special profile repository** (`Itssilviaflores/Itssilviaflores`). Because the repository name matches the owner's username, the contents of `README.md` are rendered on the owner's public GitHub profile page (https://github.com/Itssilviaflores).

It is **not** a software project. There is:

- No application or library code
- No build system, package manager, or dependencies
- No tests, linters, or CI pipelines
- No framework or runtime

The entire repository consists of a single Markdown file.

## Structure

```
.
├── README.md    # The profile content shown on the GitHub profile page
└── CLAUDE.md    # This file
```

## Content conventions

`README.md` is a short, personal "about me" intro written as a Markdown bullet list. When editing it:

- Keep the friendly, informal tone and the emoji-led bullet style already in use.
- Preserve existing external links (e.g. `www.maisonflores.com`, `www.omapadoamor.com`, `www.themapoflove.com`) unless the owner asks to change them.
- This is public-facing content on the owner's profile — treat edits like publishing. Confirm wording changes with the owner rather than rewriting voice or facts.
- Do not invent personal details, projects, or links that the owner has not provided.

About the owner (from `README.md`, for context only): Silvia Flores — founder of Maison Flores, illustrator and website builder, interested in "warm and cozy things" and building websites.

## Workflow

There is nothing to install, build, run, or test. Validating a change means previewing the Markdown renders correctly (GitHub-flavored Markdown).

Git conventions for this environment:

- Develop on the branch `claude/claude-md-docs-4jzu3g` (create it locally if missing).
- Commit with clear, descriptive messages.
- Push with `git push -u origin claude/claude-md-docs-4jzu3g`.
- Do **not** open a pull request unless the owner explicitly asks for one.
- Never push to `main` or any other branch without explicit permission.

## If this repository grows

If real project code is ever added here, update this file to document the actual structure, build/test commands, and conventions — and remove the "not a software project" framing above.
